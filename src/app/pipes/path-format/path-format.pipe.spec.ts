import { PathFormatPipe } from './path-format.pipe';

describe('PathFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new PathFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
